'use client';

import { ScrollToTop as DotoriScrollToTop } from 'dotori-components';

const ScrollToTop = () => (
  <>
    <DotoriScrollToTop className="bottom-16 z-20 h-12 w-12 p-2" showedMinHeight={0} />
  </>
);

export default ScrollToTop;
